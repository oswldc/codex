<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Remove visible background: make foreground fully transparent so
         splash background (launch_background) shows through. If you have a
         separate foreground image (transparent PNG of the logo), prefer
         using that in mipmap/ic_launcher_foreground.png instead. -->
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
